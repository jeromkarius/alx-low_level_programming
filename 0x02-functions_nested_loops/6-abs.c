#include"main.h"
/**
  * _abs - compute the absolute value of int
  * @ab : int value
  * Description : compute value
  * Return: Absolute value of ab
 */

int _abs(int ab)

{
return (ab * ((ab > 0) - (ab < 0)));
}
